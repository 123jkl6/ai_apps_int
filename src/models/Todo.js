class Todo {
    constructor(
        id, 
        title,
        summary,
        isFav, 
        labels, 
        attachments, 
        createDate,
        createTime,
        todoDate,
        todoTime,
    );
}

export default Todo;